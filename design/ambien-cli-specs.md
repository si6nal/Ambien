# `ambien-cli` Specs

## Requirements
- [ ] Create default settings file if no arguments are passed

### Commands
- [ ] Help
    - `-h` `-help`
    - Listing transformers, getting transformer specific info (descrption, tags, stability, etc.)
    - Print cli arguments
- [ ] Settings
    - `-s` `-settings`
    - Used for supplying a settings file
# `ambien-gui` Specs
Todo: (exact specs, requirements & design)

## Layout
- Tabs at the top for "Transformers" & "Settings" 
    - The "Transformers" tab will be for configuring transformer settings
        - Transformers will have their name & tags beneath the name on the left & a checkbox on the right w/ an icon indicating a dropdown for settings for the transformer (if applicable).
    - The "Settings" tab will be for core settings (input/output files)
- Beside the tabs on the right, a button that looks like a tab to run the obfuscator